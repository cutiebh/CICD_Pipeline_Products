CLone this branch.

In CLI:
# Build docker image
docker build -t cicd_pipeline_products

# List docker images
docker images

# Run docker container
docker run -p 5000:5000 cicd_pipeline_products

# Browse the URL. Add, update, delete products
http://localhost:5000

